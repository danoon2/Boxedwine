// Use the emulator's existing ZIP reader so validation and launch agree on format.
// The included source retains its upstream zlib license.
#include "../../../../../lib/zlib/contrib/minizip/unzip.c"
