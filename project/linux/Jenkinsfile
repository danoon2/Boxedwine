pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               sh '''#!/bin/bash

                    cd project/linux
                    sh build.sh || exit
                '''           
            }
        }
        stage('Test') {
            steps {
                sh '''#!/bin/bash
                    cd project/linux
                    sh buildtest.sh || exit
                    ./bin/boxedwineTest || exit 
                '''
            }
        }
    }
}