/*
 * GLES guest stub.
 *
 * This intentionally builds from the same generated pass-through source as
 * tools/opengl/libGL.so so GLES entry points use the same BoxedWine call
 * indexes from tools/opengl/gldef.h and tools/opengl/gldef_ext.h.
 */
#include "../opengl/gl.c"
